# Field Notes

`signal-mob-sync-field` is easiest to review by starting with the fixture, not the prose.

The domain cases cover `form pressure`, `sync drift`, `local state`, and `conflict cost`. They sit beside the smaller starter fixture so the project has both a compact scoring check and a domain-flavored review check.

`baseline` is the strongest case at 231 on `form pressure`. `recovery` is the cautious anchor at 197 on `conflict cost`.

The point is not to make the repository bigger. The point is to make the important judgment testable.
